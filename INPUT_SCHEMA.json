{
    "title": "playground input schema",
    "description": "This is actor input schema",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "url": {
            "title": "start url",
            "type": "string",
            "description": "first page to crawl",
            "default": "http://example.com/",
            "editor": "textfield",
            "prefill": "https://ianrackson.com/"
        },
        "customData": {
            "title": "custom data",
            "type": "object",
            "description": "user data particular to scrape",
            "editor": "json"
        }
    },
    "required": ["customData"]
}
